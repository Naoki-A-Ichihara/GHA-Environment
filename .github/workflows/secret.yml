name: Secret-test
on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: qas
    steps:
      - run: echo ${{ secrets.HOGE }} | sed -e 's/\(.\)/\1 /g'
